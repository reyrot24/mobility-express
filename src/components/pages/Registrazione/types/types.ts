export type provincetypes = {
  nome: string;
  sigla: string;
};

export type comunitypes = {
  codice: string;
  comune: string;
  cap: [];
};

export type toponomie = {
  value: string;
  name: string;
};
